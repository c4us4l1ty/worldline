# Worldline — Master Plan (Phases)

> **Purpose:** single source of truth for *what is done* vs *what is left* to reach
> MVP + production. For future AI coding agents: work phases in order,
> check off `- [x]` as you finish, keep `cargo test --workspace` (164 tests) +
> `cargo test --manifest-path crates/wl-app/Cargo.toml` (17 tests) +
> `cargo test -p wl-ui` (11 tests) green, `clippy` warning-free, `fmt` clean.
> Record every PRD deviation in `docs/PRD-DELTAS.md` (architecture rule).
>
> **How to use:** each Phase has `Done` (do not redo) and `TODO` checkboxes.
> Finish all `TODO` in a phase before moving on, unless marked `PARALLEL-OK`.
> Reference file:line for every claim so the next agent can verify.
>
> **Checkbox legend:** `- [x]` closed and verified · `- [~]` **partially** closed
> (read the per-clause status inside before assuming anything) · `- [ ]` open.
> `[~]` was introduced by the 2026-09-26 audit; treat it as "do not re-tick
> without finishing every listed clause".
>
> **Repo map (2026-09-26 audit, re-verified end-to-end):**
> `crates/wl-core` (platform-clean: crypto/HLC/CRDT/store/engine/AI/net, 112
> unit + 8 adversarial tests) · `crates/wl-protocol` (wire types, 6 tests) ·
> `crates/wl-relay` (Axum blind-blob relay, sqlite default / postgres feature,
> 11 + 8 tests) · `crates/wl-sync` (outbox drain + pull/apply + merge, 11 + 8
> tests) · `crates/wl-app` (Tauri v2 shell, EXCLUDED from workspace, 17 tests)
> + `crates/wl-app/ui` (Dioxus wasm, standalone workspace, 11 tests).
> Total: **192 tests green** (164 workspace + 17 shell + 11 UI).
> Counts verified 2026-09-26; the previous 162/15/10 = 187 figures in this
> header, in `README.md` and in `AGENTS.md` were all stale — see Phase 0.
>
> **MVP definition (updated per user changes):** open app → **Home page** (Stackelberg single directive; if no task → "No active directive."); hamburger top-left opens left slide drawer (App name top, horizontal buttons bottom: Goal creation LEFT with creation icon + contrasting/highlight, Settings RIGHT with gear icon; dimmed overlay behind drawer, drawer NOT full-screen); Settings holds 12-word phrase save + API key entry; providers strictly = Openrouter, Google, Qwen, bytez.com (Anthropic + OpenAI removed); goal creation → active directive; complete/check-in/dormant; sync. **The MVP is NOT built today.**

---

## Phase 0 — Repo hygiene & spec recovery (do FIRST, 1–2h)

### Done
- [x] Root workspace (`Cargo.toml`) = wl-core/wl-protocol/wl-relay/wl-sync; wl-app + ui deliberately standalone (webkit/gtk + wasm isolation).
- [x] `scripts/build-ui.sh` + `scripts/prune-dx-dist.sh` wired to `beforeBuildCommand`; `build.rs` fails shell build on missing dist; boot paint `#131312`; `dx build --release --debug-symbols false` pinned (PRD-DELTAS #61–62, #71–72).
- [x] `scripts/setup-linux.sh`, `scripts/setup-fedora.sh`, `scripts/smoke-native.sh`, `scripts/dx.sh` exist.
- [x] `docs/PRD-DELTAS.md` deltas #1–76 current through the Phase-1 fixes pass (2026-09-19).
- [x] Design skill binding: `.opencode/skills/worldline/SKILL.md` (435 lines).

### TODO
- [x] **P0-HYGIENE-2 — Fix stale test-count docs.** `README.md:119-123` + `AGENTS.md:6` claim "80 tests"; truth is 187 (162+15+10). Update both + `Plan/AGENT.md` (currently stale: references `cargo check -p wl-app --lib`, "Node/Webview frontend" — UI is Rust/Dioxus→wasm, shell has no lib target).
- [x] **P0-HYGIENE-3 — Pin verification gates in this file.** Canonical gates: `cargo test --workspace` · `cargo clippy --workspace --all-targets` · `cargo fmt --all -- --check` · `cargo test/clippy --manifest-path crates/wl-app/Cargo.toml` · `cd crates/wl-app/ui && cargo test -p wl-ui` + `cargo check --target wasm32-unknown-unknown`. CI-free repo: every agent runs these before declaring a phase done. (Verified 2026-09-18: 164+17+10 green, clippy/fmt clean both manifests.)
- [x] **P0-HYGIENE-4 — Count drift re-audit (2026-09-26).** Every gate re-run end-to-end; ALL FIVE PASS. Truth is **192 = 164 workspace + 17 shell + 11 UI**, not the 187/162/15/10 that this file, `README.md:128-132` and `AGENTS.md:6` all carried. Root cause of the drift: test-count figures were hand-copied forward per pass instead of re-derived from `cargo test`. All three docs now read 164/17/11/192; `wl-core` unit tests are 112 (this header previously said 120). **Standing rule: never hand-edit these numbers — copy them from the gate output of the run you just did.**
- [x] **P0-HYGIENE-5 — Two false security claims in user-facing copy (2026-09-26).** Both asserted properties the code does not have; fixed and gated behind a doc-truth audit:
  - **"Keys never touch the DOM"** (`settings.rs:314`, `byok.rs:100`) was untrue — `settings.rs:311` binds the key into an `<input value>`, and a webview cannot receive typed input any other way. A user-typed secret necessarily transits the DOM. Reworded to the guarantee that actually holds: *sealed into the vault, cleared from the field, never written to SQLite, never sent to the relay.*
  - **"Hardware keychain (Stronghold) · enclave locked · Argon2id · 128-bit salt"** (`telemetry.rs:58-59`) was false on all three counts. Verified against `iota_stronghold-2.1.0`: the snapshot cipher is **XChaCha20-Poly1305** (`src/internal/provider.rs:7`), there is **no Argon2id anywhere in the dependency**, the snapshot KDF work factor is deliberately **0** (PRD-DELTAS #20 — the key is a 32-byte CSPRNG secret, not a password), and `Vault::open` (`vault.rs:47`) constructs Stronghold with the key and **never calls `lock()`** — so "enclave locked" was false too. The vault is a local encrypted file, not hardware-backed (SHELL-6). `vault.rs:1-14`'s header carried the same two errors and was corrected.
  - **Residual worth a follow-up:** `AGENTS.md`'s design-system rule still reads "Secrets (mnemonic, BYOK keys) never touch the DOM or SQLite." The SQLite half holds; the DOM half is unachievable for user-typed input. That rule is binding, so it is left as-is pending an owner decision — the honest rule is "never reach SQLite, the relay, or logs."

---

## Audit 2026-09-26 — findings with no home in the phases below

Full end-to-end re-verification of every gate plus a claim-by-claim audit of
this file. **No ticked `[x]` item anywhere in this plan is a false claim** —
every closed item has both real code and a real regression test behind it.
The audit's output was instead: (a) 7 items marked `- [ ]` that are already
done (now re-scoped in place), (b) 3 factually wrong premises (corrected in
place), and (c) the list below — real defects the plan never tracked. Assign
each to a phase before working it.

- [ ] **AUDIT-1 — `Screen::ByokSetup` is an unreachable screen.** No inbound navigation to it exists anywhere (`app.rs:190` declares the variant, `app.rs:344` is the only transition *into* the enum, nothing routes *to* it). Its provider-pill component (`byok.rs:77-85`) is therefore dead code, and it is one half of UI-5's still-unreconciled "unify provider component" (`settings.rs:268-280` uses a `<select>` instead). Decide: delete the screen + pill group, or wire a route. Do not leave two divergent provider pickers.
- [ ] **AUDIT-2 — `wl-sync/src/sync.rs:700-707` docstring contradicts its own validator.** The docstring still says the pull validator checks a "table allow-list", but B-008 removed that allow-list and the NOTE at `:732-735` says so explicitly. A future agent trusting the docstring could "restore" a check that would re-break forward-compat. Fix the docstring.
- [ ] **AUDIT-3 — `Repos::delete_*` tombstone writers have no `wl-core` unit test.** B-003 is closed on integration coverage only (`wl-sync/tests/convergence.rs:286-384`); the six writers at `repo.rs:690-790` have no direct test. Also worth noting: the "stays gone after re-pull" leg of that integration test is a weak assertion — B's re-pull is `(0,0,0)` because the zombie op sits *below* B's cursor and is never delivered, so the real anti-resurrection proof is replica C's head-based LWW loss, not B's re-pull. The plan already discloses this; a `wl-core` unit test would make it airtight.
- [ ] **AUDIT-4 — `AGENTS.md`'s secret-handling rule is unachievable as written.** It states "Secrets (mnemonic, BYOK keys) never touch the DOM or SQLite." The SQLite half holds. The DOM half **cannot** hold: user-typed secrets enter a webview `<input>`, and the mnemonic is displayed once by design. This is the same false premise that produced the P0-HYGIENE-5 copy bug, so it will keep regenerating. **Needs an owner decision** — the honest rule is "never reach SQLite, the relay, or logs". Left unmodified because `AGENTS.md` is a binding design-system doc and rewriting its rules is not an audit's call.
- [ ] **AUDIT-5 — `SHELL-1`'s true blast radius is ~65 poison sites, and `wl-relay` is in it.** The corrected count and the full file:line list are recorded in the SHELL-1 entry above. Flagged separately because the relay is a long-lived server: `wl-relay/src/store.rs` (4 sites) and `wl-relay/src/auth.rs` (6 sites) fail-stop the whole relay on one poisoned `Mutex`, and `wl-core/src/hlc.rs` (4 sites) is on the hot path for every row mutation. Prioritise the relay + hlc subset; the `repo.rs` bulk is lower urgency.
- [ ] **AUDIT-6 — `wl-core` has zero `debug_assert` statements.** `grep debug_assert` across all of `crates/wl-core/src` returns **0 matches**, yet the plan asks for `debug_assert`s in at least three places (CORE-7(e) `reschedule_directive as i64` at `repo.rs:891`; CORE-7(g) unlocked-but-`None` identity; SHELL-2 async-context guard). Establish the convention — or decide these should be real `Result`s — before the first one lands, otherwise the next agent will keep deferring them.
- [ ] **AUDIT-7 — `estimate_adjustment` has no consumer at all.** Recorded under CORE-2/MVP-3 for the product question, but the mechanical fact is sharper than the plan states: `velocity.rs:21,82`, `commands.rs:615` and `app.rs:129` all carry the field, and **not one writer reads it** — `repo.rs:377-426` writes `estimated_minutes` verbatim and `dispatch.rs:415-421` sums phase minutes with no factor. It is currently a number that is computed, shipped to the UI, displayed honestly as "observed", and then dropped on the floor. Either wire it in or delete the field end-to-end; keeping it for display only is legitimate but should be a recorded decision, not an accident.

---

## Phase 1 — P0 bugs blocking ANY MVP demo (do SECOND, must all close)

These are not polish — each one bricks a primary user path today.

- [x] **B-001 (P0) — Provider list mismatch / Anthropic+OpenAI removal.** `crates/wl-app/src/commands.rs:853-859` allow-list must become ONLY `openrouter|google|qwen|bytez.com` (`bytez.com` added); `wl-core/src/domain/mod.rs:370` `KNOWN_PROVIDERS` must match exactly those 4; `base_for` needs `bytez.com` arm (`commands.rs:910-913`); UI provider pills (`byok.rs`/`settings.rs`) must drop Anthropic/OpenAI and show only the 4 approved. Flow broken when saved provider isn't recognized → all AI calls fail. Fix: align shell/core/UI/provider adapter matrices to the 4-only set + add `bytez` endpoint mapping + regression test (key save → master_plan with mocked `execute` succeeds for each of the 4).
- [x] **B-002 (P0) — Manual goal is a dead end (no-AI MVP path broken).** `GoalCreate.create_manual` calls only `create_goal` then routes to Canvas (`ui/src/screens/goal_create.rs`). `create_manual_milestone` / `create_manual_directive` shell commands exist but have **zero UI callers** (grep confirms). Result: manual goal has 0 milestones → `next_runnable` = none → canvas shows "The line is clear", velocity 0/0, no path to add work. Fix (pick one, record delta): (a) milestone+directive authoring UI on GoalCreate/Canvas-empty state, or (b) auto-seed one milestone + one directive from the goal title on manual create. Must be usable with NO API key (PRD delta #6).
- [x] **B-003 (P0) — Deletes never sync (sync correctness hole).** In-memory `wl-core/src/crdt/mod.rs:120-143` fully supports `tombstone` + resurrection, but `wl-sync/src/sync.rs:235` hardcodes `tombstone:false`, `apply_op_to_db` has no `DELETE` branch, and no `Repos` delete path enqueues a tombstone op. Deletes are local-only and resurrect on next pull. Fix: add `Repos::delete_*` tombstone writers + pull-apply `DELETE` branch + convergence test (delete on A → pull on B → row gone, stays gone after re-pull). (Closed commit `9a2eb99`: `Repos::delete_*` tombstone writers `repo.rs:633-790`, `apply_delete` + `record_heads` merge memory `sync.rs:344-520`, convergence test `convergence.rs:287-368` — delete on A → pull on B → row gone, stays gone after re-pull, stale upsert cannot resurrect (verified on fresh replica C; B's cursor already passed the probe HLC, see late-arrival gap under B-009). Residual: no shell command exposes deletes to the UI yet — tombstone plumbing is core/sync only.)
- [x] **B-004 (P0) — Locked-with-lost-phrase dead end offers an impossible button.** Boot locked → `SeedVault{restore:true}` → "Create new instead" → `identity_generate` always fails `identity already exists` (`commands.rs:31`). Correct cryptographically, wrong UX. Fix: replace button copy when `identity_status.has==true` with "This install already has an identity — restore the 12 words or wipe the data dir", link to wipe instructions. Never offer an action that can never succeed. **Re-verified closed 2026-09-26, but the guard is currently UNREACHABLE dead code** — `seed_vault.rs:250-253` renders the corrected copy and `:254-268` gates the impossible button behind the `else`, but `settings.rs:104` hardcodes `has_identity: false` and `restore: true` is **never set anywhere in the UI**, so the fixed branch cannot fire today. There is also **no test** for it. A future `restore:true` route must remember to pass the prop, or this bug silently returns.
- [x] **B-005 (P1→P0) — MorningBrief discards IDs / assumes persistence.** `morning_briefing` returns `Vec<String>` titles; UI navigates to Canvas assuming shell persisted directives (`ui/src/screens/morning_brief.rs`). If dispatcher didn't persist (no key, offline, validation fail), Canvas shows stale/empty with no error. Fix: surface `pending/pushed/pulled` + directive count, re-fetch `current_directive` + `velocity` after briefing, show explicit empty-state CTA to GoalCreate. (Closed 2026-09-18: `BriefingResult.created_ids` flows from `persist_briefing` through the dispatcher (`wl-core/src/ai/dispatch.rs:157-164,580-585`); shell returns `BriefingView { titles, created_ids }` (`commands.rs:991-1045`); UI surfaces `persisted of authored` count, re-fetches `current_directive` + `velocity`, runs one `sync_now` after the briefing to surface `pushed/pulled/pending` in the HUD (LOCAL on no-relay — honest, no error flash), and shows an explicit empty-state CTA to GoalCreate/Settings when nothing persisted (`ui/src/screens/morning_brief.rs`). Shim mock updated to the new contract. Parse contract unchanged: `created_ids` is `#[serde(default, skip_serializing)]` — dispatcher tests needed no assertion edits.)
- [x] **B-006 (P1) — Evening check-in unreachable except ≥19h.** `ui/src/screens/canvas.rs:70-77` gates `Evening audit` pill on `hour>=19`; no other route to `EveningCheckIn`. Fix: always-visible entry (telemetry drawer + settings + canvas overflow) while keeping the ≥19h nudge. (Closed 2026-09-18: telemetry drawer now has an always-visible `Evening audit` button (`ui/src/screens/telemetry.rs:103-110`); the ≥19h canvas pill nudge stays as-is (`canvas.rs:42-43,:81-88`). **Correction 2026-09-26: the body text above promised three routes; only the telemetry one was built** — `settings.rs` has no Evening audit link and there is no canvas overflow menu (`EveningCheckIn` appears only at `telemetry.rs:107`, `canvas.rs:85`, `app.rs:193,347`). The reachability goal is met, so this stays closed, but do not read the body as a description of what shipped.)
- [x] **B-007 (P1) — Hotkey unregister-before-validate kills summon.** `crates/wl-app/src/hotkey.rs:22-28` does `unregister_all()` then `on_shortcut(hotkey)`; malformed/conflicting input leaves NO hotkey until restart. Fix: validate/parse before unregistering, re-register `DEFAULT_HOTKEY (alt+space)` on failure. Also verify `capabilities/main-capability.json` (only `core:default`) needs no `global-shortcut` permission entry on Tauri v2.
- [x] **B-008 (P1) — Unknown-table handling wedges sync (contradicts quarantine philosophy).** `apply_op_to_db` returns `Ok(())` (ignore + advance), but `validate_pull_response` rejects unknown tables with `Protocol` abort BEFORE any apply (`wl-sync/src/sync.rs`). A future-schema peer wedges sync instead of skipping. Fix: demote unknown-table to quarantine-skip (watermark + `quarantined++`), matching poison-op path. Regression test: unknown-table op → cycle completes, cursor advances. (Closed commit `9a2eb99`: pull loop quarantine-skips unknown tables before any apply (`sync.rs:155-165`), validator NOTE documents no allow-list (`sync.rs:707`), relay push boundary rejects unknown tables 400 so poison never enters storage (`wl-relay/src/lib.rs:201`). Regression: `defect_pulled_ops_bypass_sealed_size_and_table_bounds` — unknown-table op → cycle completes, cursor advances, `(pulled,applied,quarantined)=(1,0,1)`; size bounds still abort.)
- [x] **B-009 (P1) — SQL LWW lacks the in-memory tertiary tie-break.** `TableState::apply` arbitrates `(HLC, device, operation_id)` (PRD-DELTAS #65), but `apply_op_to_db` guards only `hlc_timestamp < op.hlc` (except `check_ins` which adds `id<`). Identical-HLC/different-op_id races (forked data dirs, same device) converge order-dependently in SQL. Mitigated today only by boot jitter (0–255 counter bump, `repo.rs`). Fix: extend SQL guards to `(hlc, operation_id)` ordering everywhere, backfill test with forked-DB fixture. (Closed commit `9a2eb99` via `record_heads` merge memory: `apply_op_to_db` orders on `(hlc, device, operation_id)` against stored heads (`wl-sync/src/sync.rs:344-380`) — forked-DB fixture `equal_hlc_ops_converge_regardless_of_delivery_order` green; check-ins keep date-aware winner-take-all with same tie-break (`sync.rs:556-600`). DEFERRED RESIDUAL (new finding, 2026-09-18): exclusive composite-cursor pagination means an op whose HLC sits BELOW a replica's already-advanced cursor is permanently unreachable for that replica (probed by `delete_replicates_and_stays_deleted`'s zombie leg on device B). Convergent for same-order peers. **RESIDUAL — severity corrected 2026-09-26, it is WORSE than "after a hard-stop": the ordinary offline-catch-up path reproduces it with no hard stop at all** (A writes at `h1` while offline, B advances past `h2`, then A pushes — B never sees A's op). Root cause is the relay's exclusive composite predicate (`wl-relay/src/store.rs:256-261`: `AND (hlc > ?2 OR (hlc = ?2 AND operation_id > ?3))`) combined with the fact that **no shell path ever resets `sync_cursor`** (telemetry's "Force CRDT peer re-sync" is just another `sync_now`; no `force_resync`/`save_cursor("")` exists in `crates/wl-app/src`). "Permanently" is therefore literal. Note `wl-sync/tests/adversarial.rs:362` `defect_same_hlc_ops_lost_by_strict_cursor` documents only the *equal*-HLC half — ties ARE reachable via the composite cursor, so that test is **not** evidence about the below-cursor gap. Fix requires arrival-sequence cursors or inclusive re-delivery — a SYNC-6-grade protocol change, tracked there.)

**Exit criteria:** fresh profile can complete the full MVP path with NO API key (manual goal → directive → complete → check-in → sync across 2 app data dirs via local relay). Record a screen-by-screen transcript in the PR.

---

## Phase 2 — MVP flow completion (the "not built" gap)

### Done
- [x] FSM screens updated: Boot → Home page (single directive; no onboarding at open; "No active directive." when empty) → Hamburger drawer (left slide, dimmed full-screen overlay, drawer NOT full-screen; App name top, 2 horizontal buttons bottom: Goal creation LEFT with creation icon + contrasting/highlight, Settings RIGHT with gear icon) → Settings (12-word phrase save + API key + providers: Openrouter, Google, Qwen, bytez.com) → Canvas/DirectiveActive → CheckIn/Dormant; SeedVault/mnemonic moved to Settings (not at boot); ByokSetup removed from boot flow; GoalCreate triggered from drawer. No Anthropic/OpenAI providers anywhere.
- [x] Shell 24 commands cover identity/BYOK/authoring/canvas/settings/sync/AI/window (`main.rs:56-81`, `commands.rs:1105`). Arg shapes correct in UI (wrapped `{"settings":{…}}`, flat `reason/note`, `indices/words`, etc.).
- [x] Escape modal categorized (Blocked/Scope/Energy, `1/2/3` + `Esc`, 140-char cap); engine `bail_out` downsizes/rescales (PRD-DELTAS #31).
- [x] Single-active invariant enforced + self-healed (`set_directive_state` parks others, `enforce_single_active` after pull, `active_directive ORDER BY hlc DESC, id`).
- [x] Manual fallback without key approved (PRD delta #6); BYOK skip allowed.

### TODO
- [x] **MVP-1 — Authoring UI (drawer button + empty-state).** (CLOSED 2026-09-26 — was stale-unticked.) Drawer opens left slide with dimmed overlay; App name at top; bottom 2 buttons HORIZONTAL: Goal creation LEFT (coral `#E26D52` + `#131312` `✚`) → GoalCreate; Settings RIGHT (`#2A2A29` pill + `#949087` `⚙`) → Settings. Sheet is NOT full-screen. Evidence: `ui/src/screens/nav_drawer.rs:39-61` (App name `:45`, create `:48-54`, settings `:55-61`, both with `aria_label`), `wl.css:701-711` (`.wl-nav-sheet` `width:65%`/`max-width:273px`, `#20201F`, rounded right edge only), `wl.css:696-700` + `:444-447` (`.wl-modal-backdrop` `position:absolute; inset:0; background:rgba(19,19,18,0.85)`), `wl.css:724-728` (`.wl-nav-actions{flex-direction:row}`), `wl.css:744-753` (button colors), close paths `nav_drawer.rs:38` (backdrop) / `app.rs:327-330` (Esc) / `nav_drawer.rs:24-27` (tap). Empty state `canvas.rs:142` exact copy "No active directive." + `:147` "Open menu" CTA. **Two design-system drifts remain** (not blocking): app name renders `--wl-text-primary` `#E5E2E0`, spec says `#DAD5C7` (`wl.css:721`); settings pill is `#2A2A29`, spec says `#20201F`. Icons are emoji glyphs, so AT reads glyph + `aria_label` — fine, but a vector icon set would be cleaner.
- [x] **MVP-2 — Briefing→Canvas contract.** (CLOSED 2026-09-26 — shipped under B-005, left stale-unticked.) Shell returns `BriefingView{titles,created_ids}` (`commands.rs:998-1007`, returned `:1042-1048`); UI re-fetches `current_directive` (`morning_brief.rs:54`) + `velocity` (`:58`); 0-created case has distinct copy + GoalCreate/Settings CTA (`:136-153`); 1/2/3 render the "persisted of drafted" count (`:154-176`). Never a blank canvas.
- [~] **MVP-3 — Check-in→velocity→dormant loop.** (PARTIAL 2026-09-26.) ✅ `velocity()` **is** refreshed after `complete_directive` (`canvas.rs:489-494`) — the plan's first clause landed. ✅ `estimate_adjustment` display is now honest: `checkin.rs:102` labels it "observed adjustment {pct}% (not yet applied to future estimates)" and `:108` says so explicitly; recorded as PRD-DELTAS #75. ❌ The value itself is still neither applied nor renamed — the field is still `estimate_adjustment` end-to-end (`velocity.rs:21,82`, `commands.rs:615`, `app.rs:129`) and **no writer consumes it** (`repo.rs:377-426` writes `estimated_minutes` verbatim). MVP-3's *honesty* half is done; the underlying defect is **CORE-2/E7** and stays open there. GPS copy in CheckIn confirmed present.
- [~] **MVP-4 — Sync UX.** (MOSTLY CLOSED 2026-09-26 — 4 of 5 sub-items landed.) ✅ "Test connection" wired: `settings.rs:411` invokes `relay_authenticate`, button at `:434`, single-in-flight guard `:15,:397`, DTO `{account_id,expires_at}` matching the real `RelayAuthView` (`commands.rs:745-749`). ✅ `SyncStatsView` now carries `quarantined` (`:879`) and `cursor` (`:881`) — the plan's "omits quarantined/cursor" premise was stale. ✅ HUD `LOCAL/SYNCED/OFFLINE` reflects last-sync age honestly (`app.rs:467-479`, "LOCAL · never synced" when no cycle has run). ✅ "Sync now" exists — as a **HUD pill** (`canvas.rs:96-121`, `⇅`), not a canvas overflow menu; the plan's "add to canvas overflow" wording was never the mechanism chosen. ❌ `applied` is the one stat still unsurfaced (telemetry shows pushed/pulled/quarantined/pending/cursor, `telemetry.rs:70-71`). No background auto-sync — remains documented on-demand, as intended.
- [x] **MVP-5 — Boot/home-page recovery + settings identity flow.** (Closed 2026-09-18: `c31b9d3` — `BootError` screen, boot routes to Home/Canvas directly, identity unlocked via Settings, `identity_status` signal shared across drawer/canvas/settings, mock shim mirrors shell sync/auth shapes.) Re-verified 2026-09-26: `app.rs:287-313` routes to `Screen::Canvas` regardless of identity, `BootError` only on shell error; identity flow `settings.rs:76-192`.
- [~] **MVP-6 — Tauri shape audit.** (PARTIAL 2026-09-26 — static half done, live gate unrun.) ✅ 2 of the 3 named shim divergences are **fixed**: `relay_authenticate` shim now returns `{account_id, expires_at}` (`invoke-shim.js:142-147`, matches real `RelayAuthView`) and `sync_now` now returns all six fields incl. `quarantined`/`cursor` (`:141`). ❌ mock ids remain `ms-`/`dir-`+`Date.now()` (`:139-140`) but are harmless — browser-only, and the UI has zero callers of those two commands. ✅ **Full static arg-shape audit passed**: `settings_save` correctly wrapped `{"settings":{…}}` (`settings.rs:52,:247`, `byok.rs:158`) vs `commands.rs:657-660`; flat shapes verified for `bail_out{reason,note}`, `check_in{outcome,note}`, `set_always_on_top{pinned}`, `identity_restore{phrase}`, `set_api_key/has_api_key/delete_api_key{provider,key}`, `identity_verify_backup{indices,words}`, `master_plan`, `morning_briefing`, `create_goal`; `AppSettingsView` field names == `AppSettings`. ❌ **The plan's actual requirement — re-verify EVERY command against the real shell via `cargo tauri dev` — is a runtime gate and has still not been run.** Static signatures agree; do NOT tick this closed until a live transcript exists.

**Exit criteria:** Phase 1 exit + AI path works for the 4 providers ONLY (`openrouter|google|qwen|bytez.com` adapters live with mocked + sandboxed HTTP); drawer opens correctly; boot routes directly to Home with "No active directive." empty-state; Settings holds phrase + API key + provider selection; `dx serve` mock and real shell agree on all 24 commands with updated provider matrix.

---

## Phase 3 — Sync + relay hardening (what's left after 3 battle passes)

### Done (PRD-DELTAS #21–33, #39–50, #55–69 — do not regress)
- [x] Fixed-width HLC `pt(20).ctr(5).dev(5)`; composite `(hlc,op_id)` cursor + per-batch persist; whole-outbox push drain + `accepted+duplicates→pushed→DELETE`; LWW-guarded pull apply incl. `app_settings`; persisted `hlc_clock` head + counter saturation; poison-op + constraint-conflict quarantine; HLC receive events; single-active self-heal; 200-batch loop bounds + `batch_limit 1–500`; transactional outbox drain; `crdt_applied` prune + drained-outbox delete; per-account `(account,op_id)` dedup; empty-batch cursor-integrity abort; relay push validation (HLC round-trip, 7-table allow-list, 256 KiB, 500/req, 100k/account); relay `spawn_blocking` (no executor block); SSRF blocklist + relay-URL allow-list; shared reqwest client; `MAX_BATCH_OPS/SEALED_B64/HEADER/REQUEST/RESPONSE` enforced.

### TODO
- [ ] **SYNC-1 (P4, characterized) — Relay push holds one txn/mutex across whole batch.** `wl-relay/tests/adversarial.rs:275-278` `defect_push_holds_global_mutex_across_whole_batch` locks the behavior. Fix: chunked commits (e.g. 100 ops/chunk) so pulls aren't convoyed behind 20k-op pushes. Keep idempotence + quota atomicity.
- [ ] **SYNC-2 — Per-account 100k op cap is a cliff.** Pushes 429 even critical writes; ops accumulate forever (no GC/TTL/eviction/compaction/VACUUM). Design + implement retention (e.g. LWW-compacted snapshot or TTL + client re-bootstrap), or at minimum a `Retry-After` + "contact admin / rotate account" error path + ops-count telemetry.
- [ ] **SYNC-3 — Postgres/SQLite divergence.** PG index `idx_ops_account_hlc(account,hlc)` lacks `(operation_id)` tail vs SQLite; `register_account LOCK TABLE` path needs prod verification via `docker compose up` + `WL_RELAY_PG_URL`; `main.rs` feature-combo fragility (both-features → SQLite silently wins; no-features → unbound `blobs`): add `compile_error!` guard.
- [ ] **SYNC-4 — Size-bound skew.** (Re-verified open 2026-09-26; skew is exactly as described and still un-unified.) Relay body cap `DefaultBodyLimit::max(MAX_REQUEST_BYTES = 2 MiB)` (`wl-protocol/src/lib.rs:14`, applied `wl-relay/src/lib.rs:53`); per-op sealed 256 KiB (`wl-protocol:11`, enforced `lib.rs:229-230`); shell pull limit **16 MiB** (`commands.rs:740` `relay::read_body(resp, 16 * 1024 * 1024)`); handshake **64 KiB** (`relay.rs:91`). Two dead/underused constants: **`MAX_RESPONSE_BYTES = 4 MiB` (`wl-protocol:15`) is referenced only by a text validator** (`wl-sync/src/sync.rs:694`) and **never by the relay's own response path**; **`MAX_PULL_BYTES` (1 MiB, `wl-protocol:16`) is fully dead — its definition is the only occurrence repo-wide.** Unify + enforce + test the smallest binding limit end-to-end. (Context: unbounded strings today bloat sealed ops past 256 KiB → undrainable batch; the write budgets in #64 mitigate, don't eliminate.)
- [ ] **SYNC-5 — Cursor fidelity.** `SyncStats.cursor` returns HLC only, drops `op_id`; callers can't resume exactly. Return `(cursor_hlc, cursor_op_id)` through `sync_now` → telemetry.
- [ ] **SYNC-6 — Forward-compat policy.** Decide once: unknown-table/oversize-sealed = quarantine-skip (resilient) vs abort (strict). Today pull-validator aborts, apply ignores — pick quarantine (see B-008) and document. (B-008 side decided: quarantine-skip, landed. OPEN: cursor-mode policy — B-009 residual: exclusive composite-cursor pagination permanently hides below-cursor late ops from caught-up replicas; arrival-sequence cursor or inclusive re-delivery is the fix (2026-09-18).)
- [ ] **SYNC-7 — Deferred crypto hardening (documented, not fixed).** AEAD AAD is only `table:record` — bind `operation_id`/HLC (needs migration + version gate); HLC arrival-sequence cutover; `operation_id` global-uniqueness squat (accepted: UUID prediction infeasible, but re-audit); DNS-rebinding TOCTOU (capped by E2EE blindness — document residual).
- [ ] **SYNC-8 — Opaque sessions die on restart; no TLS.** In-memory `HashMap` (clients recover via 401→re-handshake — keep); production needs reverse-proxy TLS (not in repo — add example Caddy/nginx + `WL_RELAY_ADDR` docs); unauthenticated registration (10k-account cap only — rate-limit by IP or invite code before public deploy).

---

## Phase 4 — Core engine + store correctness (Tier-3 + lifecycle)

### Done
- [x] `Engine::{activate_next,current,complete,bail_out,check_in}` + `RecoveryAction` + `velocity::compute` (EWMA α=0.4, 14-day window, `adjustment=0.6+0.4*ratio` floored 0.6); progressive phases (threshold 30m, auto-split 5m+rest, sum-rewrite, 2× band); `reschedule` resets to phase 1 + rescales; write-boundary budgets (titles ≤500, desc ≤4000, notes ≤2000, models ≤256, 1–1440m, strict `YYYY-MM-DD`, 140-char bailout); transactional row+outbox+HLC commits; `check_text/date/minutes` validators.

### TODO
- [ ] **CORE-1 (E8) — `blocked` has no unblock path; `skipped` never completes a milestone.** `engine/mod.rs:173-202,330`. Design the state machine: `blocked → (unblock → queued/active | escalate → bailout)`; define what `skipped` does to milestone progress (counts 0 today — keep, but surface "velocity adjustment" + offer requeue). Add `unblock_directive` command + UI affordance. Characterization guard exists — replace with real behavior + tests.
- [ ] **CORE-2 (E7) — `estimate_adjustment` computed, never applied.** `engine/velocity.rs:82` → `commands.rs:584-602` → `checkin.rs:59` displays but future estimates ignore it. Either apply (with 60% floor, PRD delta #16) at `create_directive` time or rename to `observed_adjustment` + delta note. Don't display a number that does nothing.
- [ ] **CORE-3 — No `set_goal_status`; multiple `active` goals accumulate.** `GoalStatus::{Active,Achieved,Archived}` exists, `Repos` has no setter (tests use raw SQL); `persist_plan` always inserts new `active` goal; `active_goal() ORDER BY hlc LIMIT 1` is arbitrary. Add `set_goal_status` + archive-previous-on-new-plan + test.
- [ ] **CORE-4 — `identity_config` allows N rows.** PK=`public_key` + `ON CONFLICT(public_key)` permits multiples; `identity() LIMIT 1` arbitrary; `set_mnemonic_verified` updates ALL rows. Make singleton (`id=1`) or delete-on-restore + test.
- [ ] **CORE-5 — CRDT poison-op mutates state on reject.** `crdt/mod.rs:120-143` inserts `last_op` + removes `tombstone` BEFORE `resurrect_ok`/`fields` checks; field-less upsert mutates then returns `false`. Fix: validate `fields.is_some()` before touching `last_op/tombstones` + regression test.
- [ ] **CORE-6 — `current_phase_minutes` fail-closed with no repair.** Progressive `total>1` + missing phase row → `Invalid("current phase missing")` breaks `activate_next/complete`/HUD. Add repair (rebuild phases from estimate) or honest "corrupt directive — requeue" path.
- [ ] **CORE-7 — Gaps.** `save_settings` never validates `relay_url` (relies on shell pre-validate — move `net::validate_relay_url` into `Repos::save_settings`); `enqueue_outbox` has no payload-size cap (relay 256 KiB — enforce at enqueue); `validate_plan` message says "2–5 milestones" but code allows 1 (`ai/dispatch.rs` — fix message OR require `len>=2`); Tier-2 `persist_briefing` has no same-day dedup (double-run duplicates today — add idempotency key); `reschedule_directive as i64` cast (safe ≤2M today — add debug_assert); prompts embed raw user text (pre-check caps exist in dispatcher — enforce in `persist_*` too); `Engine` reaches into `repos.conn` pub field (`engine/mod.rs:283,302,327` — add accessor); `None`-identity silent fork (documented — add debug_assert/log when unlocked-but-None).

---

## Phase 5 — Shell robustness (Tauri + vault + IPC)

### Done
- [x] `AppState {repos, identity: Mutex<Option<Identity>>, vault, relay_token, relay_url}`; identity-mutex serialization; `with_identity/with_identity_opt`; vault `0600`/`0700` hardening + `save_mnemonic` rollback; snapshot KDF work-factor 0 (strong key, PRD delta #20); Stronghold NOT registered as plugin (PRD delta #21); `pub(crate)` commands; 401→re-handshake→retry once; session TTL 1h; sync I/O on `spawn_blocking` only; **17** shell tests (vault round-trip/perms, session scoping, live-relay handshake, snapshot-failure atomicity, provider matrix, manual-goal seeding).

### TODO
- [ ] **SHELL-1 — Mutex-poison fail-stop everywhere.** **COUNT CORRECTED 2026-09-26: ~65 sites, not ~30.** Repo-wide: **49** `.lock().unwrap()` — `wl-core/src/store/repo.rs` ×28 (73, 86, 156, 181, 261, 293, 310, 413, 477, 498, 514, 550, 645, 737, 797, 842, 928, 989, 1043, 1112, 1150, 1183, 1236, 1407, 1425, 1448, 1498, 1510), `wl-app/src/commands.rs` ×15 (30, 69, 107, 164, 186, 361, 493, 686, 689, 766, 774, 794, 802, 833, 842), `wl-core/src/engine/mod.rs` ×3 (283, 301, 327), `wl-sync/src/sync.rs` ×3 (283, 667, 681) — **plus 16 `.lock().expect(...)` poison-panics this plan never counted**: `wl-core/src/hlc.rs:83,104,133,142`; `wl-relay/src/store.rs:169,188,198,248`; `wl-relay/src/auth.rs:90,119,127,162,183,232`; `wl-app/src/app_state.rs:67,81`. The plan's original `commands.rs` line list was also stale (`331,463,655,658` are no longer lock sites; `361,493,766,794` were missed). **`into_inner()` recovery appears ZERO times repo-wide.** All panic instead of mapping to `StoreError`/`ShellError`. At minimum, shell + relay must not fail-stop a long-lived process on one bad holder.
- [~] **SHELL-2 — `spawn_blocking` discipline is footgun-by-design.** (PARTIAL 2026-09-26 — docs landed, enforcement did not.) The footgun is intact: `relay.rs:22-27` builds a current-thread runtime and `block_on`s, same in `commands.rs:724-728` (`ReqwestTransport::post`) and `:967-971` (`http_execute`); calling these from async context panics by design. ✅ **The documentation half landed** and is accurate: `relay.rs:10-16` ("MUST only run on a thread with no Tokio runtime context… panics by design"), plus `relay.rs:126`, `commands.rs:756-757`, `commands.rs:827-828`. ❌ **Zero enforcement**: `debug_assert` count in `crates/wl-app/src` is **0**; no `Handle::current()` check; no clippy lint; no `clippy.toml` in the repo. Either add `debug_assert!(Handle::current().is_none())` at the three block sites plus a lint, or refactor to a pure-async transport.
- [ ] **SHELL-3 — Vault API-key write lacks rollback** (mnemonic has it, keys don't). Confirmed open 2026-09-26: `vault.rs:97-101` does `self.put(...)?` then `self.commit()`; on commit failure the in-memory store keeps the new value while the durable snapshot keeps the old one. Compare `save_mnemonic` (`vault.rs:65-80`), which captures `previous` and restores/deletes on commit error. No test injects a commit failure.
- [~] **SHELL-4 — API-key Zeroizing residual.** (PARTIAL 2026-09-26 — note is current, clearing is not done.) ✅ The audit note exists and is accurate: `wl-core/src/ai/dispatch.rs:38-44` ("Two residual copies are unavoidable and short-lived — the `Authorization`/`x-api-key` header strings handed to the HTTP layer, and whatever the HTTP client retains"), plus PRD-DELTAS #51. Vault→adapter is already move-not-clone (`commands.rs:893` returns `Zeroizing<String>`; `:916-928`). ❌ The residual is still clearable in one place: `dispatch.rs:99` `format!("Bearer {}", api_key.as_str())` produces a **non-zeroized** `String`, and `commands.rs:973-974` copies it into reqwest's `HeaderMap`. Fix the header to be `Zeroizing`/dropped, then re-audit.
- [~] **SHELL-5 — Boot/home-page flow (no onboarding).** (MOSTLY CLOSED 2026-09-26 — ~85% landed; 2 residuals open.) ✅ `AppState::new` does **not** trigger SeedVault (`app_state.rs:53-59`, `identity: Mutex::new(None)`; the vault is opened but never unlocked at boot). ✅ Boot routes straight to `Screen::Canvas` regardless of identity state (`app.rs:304-311`; `Ok → Canvas`, `Err → BootError`), and `identity_unlock` is called from Settings, not boot (`settings.rs:121`). ✅ `read_body` is already **fail-closed**, not underflow-panicking: `relay.rs:105` and `:110` both return `Err("response body too large")`, the `limit - bytes.len()` invariant at `:109` is provably non-underflowing, and a boundary test covers it (`relay.rs:172-190`). ❌ **No `device_id_for` repair UI** — `app_state.rs:87-100` hard-errors on a corrupt marker and the UI has zero occurrences of "device id" or "repair"; a user with a corrupt marker gets no path forward. ❌ **"Locked with no vault file" never reaches an error screen**: a `Vault::open` failure at `app_state.rs:52` propagates out of `setup` (`main.rs:35 ?`) into `.expect("Worldline shell failed to start")` (`main.rs:83`) — a hard **process panic** — and the boot effect's `settings_get(...).unwrap_or_default()` (`app.rs:297`) still silently defaults instead of distinguishing "loading" from "broken".
- [ ] **SHELL-6 — Vault is `0600` file, not OS keychain.** Confirmed open 2026-09-26 — and now the *documented* threat model, not just an internal note: `vault.rs:47-59` opens Stronghold on `dir/vault.hold` with a 32-byte CSPRNG key from `dir/.vault-key` (`0600`), KDF work factor 0. No keyring/Stronghold-OS-backend. `vault.rs:5-14` and PRD-DELTAS #19 defer it. Migrate to the OS keychain before public release; add a vault-wide key purge (today only per-provider `delete_api_key`, `vault.rs:120-127`; PRD-DELTAS #36 records "no vault-wide purge command added"). The telemetry drawer now discloses this limitation in-product (see P0-HYGIENE-5).

---

## Phase 6 — UI/UX completion + design-system compliance

### Done (frontend pass 2026-09-16 + battle-test 3 — do not regress)
- [x] Tokens (`#131312/#20201F/#DAD5C7/#E26D52`, light `#F4F0E8/#ECE6DA`), fonts bundled (DM Sans/Doppio One, no CDN), 420×747 fixed, single-directive canvas, categorized escape, GPS "velocity adjustment" copy, no streaks/red/future-list, secrets cleared post-verify/restore/save, key-probe resubscribe, pin-to-top persist+revert, double-submit guards, positional backup verify, `(hlc,id)` ordering, HUD tick gated on Canvas, `Signal<String>` sync_status, reduced-motion + focus ring.

### TODO (bugs + drifts — all in `crates/wl-app/ui/src`)
- [ ] **UI-1 — Type mismatch masked.** Shell always returns `DirectiveView` (idle `state:"idle",id:""`); UI invokes as `Option<DirectiveView>` relying on `active_directive` filter. Works, masks errors. Fix: invoke as `DirectiveView` + explicit `is_idle()` check. **Re-verified open 2026-09-26:** all three call sites still coerce — `canvas.rs:24,:329` and `morning_brief.rs:54`; `is_idle` has **zero occurrences repo-wide**; the masking filter survives as `active_directive()` (`app.rs:226-228`) applied in `set_directive` (`app.rs:397-398`).
- [ ] **UI-2 — EscapeModal inconsistency.** Backdrop click → `confirming` ("Keep directive?") while `Esc` closes instantly; recovery string (`downsized:15/advanced:…/low-cognitive`) discarded, generic toast only. Unify: Esc == backdrop (friction preserved both), toast shows actual recovery ("Resized to 15m · back on the line tomorrow"). **Re-verified open 2026-09-26:** `canvas.rs:362-364` `Esc` → `s.set(false)` (instant close) vs `canvas.rs:371-375` backdrop → `confirming.set(true)`; the recovery string from `BailoutOutcome.recovery` (`commands.rs:566-571`, built at `:575-585`) is **discarded** at `canvas.rs:324-328` in favour of a generic flash. Nothing in `ui/src` reads `.recovery`.
- [ ] **UI-3 — Settings staleness.** `local` snapshot at mount, never resyncs; theme applies before Save (diverges on abandon); pin bypasses Save while hotkey doesn't. Fix: resync on focus, Save-or-revert theme, consistent persist model, hotkey format hint + validation. **Re-verified open 2026-09-26, all three sub-clauses live:** `settings.rs:11` takes a mount-time snapshot with **no** re-invoke and **no** focus/visibility handler; `settings.rs:198-203` applies theme via `theme.rs:25-29` immediately on `onchange` and never reverts; `settings.rs:221-263` still persists pin on click (`:245-249`) while hotkey waits for Save, and the hotkey input has no placeholder, format hint, or client-side validation — a malformed string is only caught at `commands.rs:681-683` after the row is written.
- [x] **UI-4 — Drawer design + home-page discovery.** (CLOSED 2026-09-26 — was stale-unticked; fully duplicated by MVP-1, which shipped in the same pass.) All five requirements are met in code: left slide-out `65%` sheet that is NOT full-screen (`wl.css:701-711`), full-screen dimmed overlay `rgba(19,19,18,0.85)` (`wl.css:696-700` + `:444-447`), App name top (`.wl-nav-appname`, `wl.css:716-722`), the 2 buttons HORIZONTAL not stacked (`wl.css:724-728`, `flex-direction:row`), icons only with accessible `aria-label`s (`nav_drawer.rs:50,:57`), closes on Esc / backdrop / tap (`nav_drawer.rs:38`, `app.rs:327-330`, `nav_drawer.rs:24-27`). Empty state `"No active directive."` (`canvas.rs:142`) + subtle "Open menu" link (`:147`). **Stackelberg preserved — no backlog list on canvas** (`canvas.rs:137-151`). Two color drifts logged under MVP-1 above (app name `#E5E2E0` vs spec `#DAD5C7`; settings pill `#2A2A29` vs spec `#20201F`).
- [~] **UI-5 — Copy + provider + drawer icon bugs.** (PROVIDERS SOLVED 2026-09-26; rest open.) ✅ **Provider pills are done** — `grep -i 'anthropic|openai|gemini-compat'` across all of `crates/wl-app/ui` returns **zero** matches; `settings.rs:276-279` and `byok.rs:12-17` list exactly the 4 approved. ✅ **Both false security claims FIXED** (P0-HYGIENE-5): "never touch the DOM" reworded in `settings.rs:313-315` + `byok.rs:99-101`; "Hardware keychain · enclave locked · Argon2id · 128-bit salt" replaced with the verified truth in `telemetry.rs:57-63`. ❌ **Remaining:** drawer app-name/pill color drift (see UI-4); `rgba(0,0,0,…)` shadows still un-allowlisted (`wl.css:218` shadow, `wl.css:324` bg); `SKILL.md:431` still says card `#1C1C1B` vs code `#20201F` (`wl.css:21`) — the same file is correct at `:14,:28,:87`; MorningBrief 3-title stack (`morning_brief.rs:159-165`) still has no PRD-DELTAS note; **dead CSS still present** — `.wl-btn-coral` (`wl.css:656-674`) and the whole orphaned `.wl-phase-list/.wl-phase-row/.wl-phase-now/.wl-phase-glyph/.wl-phase-text` family (`wl.css:634-650`), with zero rsx users; provider component **not** unified — `byok.rs:77-85` pill group vs `settings.rs:268-280` `<select>`, and `Screen::ByokSetup` has **no inbound navigation anywhere**, so the pill variant is dead code.
- [~] **UI-6 — Telemetry honesty.** (HALF CLOSED 2026-09-26.) ✅ HLC head and WAL size now honestly render `— (no shell command)` (`telemetry.rs:65,:67`) — do not fabricate; `quarantined` + `cursor` are now inspectable from a real cycle (`:70-71`). ✅ The vault block no longer fabricates crypto properties (P0-HYGIENE-5). ❌ **Static geometry still rendered as if live:** `telemetry.rs:77` prints `"Window geometry: 420px × 747px (9:16 fixed)"` under the heading "Desktop viewport runtime" (`:76`) — a hardcoded string. The plan explicitly demanded this stop. ❌ **The `sync_debug_info` decision was never recorded** — zero occurrences repo-wide. Either add it or commit to keeping `—` permanently, and write the decision down.
- [ ] **UI-7 — A11y + i18n.** Focus-trap escape modal, `aria-live` toast, timer `aria-label`, keyboard map help (`?`). **Re-verified 2026-09-26 — nothing landed, all four absent:** `EscapeModal` (`canvas.rs:292-460`) has `tabindex:"0"` + `autofocus` (`:351-352`) but no Tab interception, no focus sentinel, no focus restore on close (`trap` → 0 hits); the toast is a bare `div.wl-toast` (`app.rs:358-360`, `aria-live`/`aria_live` → 0 hits repo-wide); the timer — the only live-updating text in the app — renders bare `rsx!{ "{timer}" }` (`canvas.rs:231`) with no label (its button carries only `title:"System telemetry (Ctrl+,)"`, `:124`); no `?` help map (bound keys `⌘↵`/`Esc`/`Ctrl+,`/`1/2/3` are shown as `kbd` chips but never documented in one place). No i18n in v1 — **still not recorded** in `docs/PRD-DELTAS.md`. Credit where due: `aria_pressed` on the 3 bailout reasons (`canvas.rs:401,407,413`) and `role`/`aria_label` on both drawers are real.

---

## Phase 7 — AI BYOK completion (Tier-1/2 + Tier-3 wiring)

### Done
- [x] Provider adapters for Openrouter, Google, Qwen, bytez.com (`ProviderAdapter` variants updated to 4 only); redacted Debug + `Zeroizing` key (moved, not cloned); `temperature:0.4 + response_format:json_object` / `max_tokens:4096`; `strip_fences/extract_json_block/validate_response_size 256KiB/validate_text 16KiB`; `validate_plan (1..5 milestones, ≤32 dirs/milestone)` + `validate_directive` (phase-sum 2× band, >30m requires phases); `persist_plan/persist_briefing`; `AiDispatcher::{master_plan,morning_briefing}` with injected `execute` (platform-clean); pre-HTTP validation (`MAX_AI_CONTEXT 16K`, `MAX_AI_CONSTRAINTS 8K`); model IDs user-configurable (`tier1_model/tier2_model`), 4-provider allow-list (`openrouter|google|qwen|bytez.com`).

### TODO
- [ ] **AI-1 — AI batch atomicity deferred.** Confirmed open 2026-09-26: `persist_plan` (`dispatch.rs:389-435`) commits the goal (`repo.rs:213`), then each milestone (`repo.rs:287`), then each directive (`repo.rs:~450`) in **its own transaction**. No single `Repos` transaction, no validate-all→commit-all phase, and no "kill mid-persist → no partial goal" test exists. Wrap in one transaction or two-phase + test.
- [~] **AI-2 — Model/provider matrix (4 providers ONLY).** (MOSTLY DONE 2026-09-26 — only the live half is missing.) ✅ **All four sources agree** on `openrouter|google|qwen|bytez.com`: `KNOWN_PROVIDERS` (`domain/mod.rs:371`), `base_for` with `_ => None` (`commands.rs:952-960`), `ProviderAdapter` down to a single `OpenAiCompat` variant with the Anthropic variant deleted (`dispatch.rs:46-55`), and the `vault_api_key` allow-list bound to `KNOWN_PROVIDERS` (`commands.rs:894`). ✅ **Mocked tests exist for all four**: `ai/tests.rs:80-105` (request shape) and `:107-166` (master_plan end-to-end), plus `commands.rs:1140-1198` driving vault→adapter→master_plan for each and asserting dead ids are rejected (`:1188-1195`). ❌ **No sandboxed-live per-provider verification exists** — `grep 'WL_AI_LIVE|ignored|env::var'` over `crates/` returns only `wl-relay/src/main.rs:20,21,33`, so there is no env-gated live AI test to flip on.
- [x] **AI-3 — Briefing context (4 providers).** (CLOSED 2026-09-26 — was stale-unticked; every sub-clause is implemented and wired.) ✅ Tier-2 prompt carries active milestone + description (`ai/prompt.rs:70-75`), velocity JSON (`:76-78`), and user constraints (`:82`); the shell serializes the live `VelocityView` into `velocity_json` and passes it (`commands.rs:1028-1029,:1035`). ✅ Providers are the 4 only. ✅ **Missing-key vs offline are distinguished in the UI** (`morning_brief.rs:82-90`: "ADD ONE IN SETTINGS" vs "OFFLINE — MANUAL MODE"), backed by `ShellError::NoApiKey` (`error.rs:27-28`), with a manual-plan escape hatch (`:130-135`). ✅ Settings-first identity flow complete: 12-word unlock/restore/generate (`settings.rs:76-190`) + provider select and `set_api_key` (`:267-340`).
- [ ] **AI-4 — Cost guards.** Confirmed open 2026-09-26: no per-day call counter and no cost-estimate display anywhere (`per-day|daily_call|call_count|cost_estimate` → only the unrelated `target_per_day` velocity field). The second clause is **vacuously** satisfied, not implemented: `commands.rs:977-979` returns `Err(format!("HTTP {}", …))` on any non-2xx with no retry loop, and `shared_client()` has no retry middleware — so there is no auto-retry of billable calls, but there is also no guard. Add the counter + pre-billable-hop estimate, and keep the no-auto-retry property explicit.

---

## Phase 8 — Verification, packaging & production readiness

### Done
- [x] 192 green (164 workspace + 17 shell + 11 UI; re-verified end-to-end 2026-09-26, superseding the stale 187/162/15/10 — see P0-HYGIENE-4); clippy clean (workspace + wl-app); fmt clean (all three manifests); `smoke-native.sh` pixel-asserts release window (stddev 104, canvas 0.26, KDE/Wayland); idle 0.01% CPU, 151 MiB RSS (claimed 2026-09-17 — re-verify, not re-run here).

### TODO
- [ ] **SHIP-1 — Human-hardware gates (cannot be done in sandbox).** `bash scripts/setup-linux.sh` → `cd crates/wl-app && cargo tauri build` → `scripts/smoke-native.sh` → attach screenshot + stddev/canvas numbers. `docker compose up -d` → Postgres relay (`WL_RELAY_PG_URL`) → two-device sync transcript. `Alt+Space` WM-collision note (Linux menu conflict — document rebind). All three before calling MVP "done". Infrastructure re-verified present 2026-09-26 (`setup-linux.sh`, `smoke-native.sh` asserting PNG magic at `:76` and printing `window {w}x{h}, channel stddev {max_sd:.1f}, canvas fraction {frac:.2f}` at `:140`; `docker-compose.yml:4-17` PG on 5433 consumed at `wl-relay/src/main.rs:33`); the **evidence artifacts are still absent** — no screenshot, no stddev numbers, no two-device transcript, and the `Alt+Space` conflict note exists nowhere (`settings.rs:210-214` is a bare input with no hint; `README.md:67` mentions Alt+Space with no caveat).
- [ ] **SHIP-2 — Security pre-release.** OS-keychain migration (SHELL-6); PASETO `v4.local` wrapper behind same `Bearer` header (PRD delta #4); per-device keys/X25519 v2 scoping (dropped in v1 delta #3 — record v2 plan, no code); metadata-host SSRF re-audit incl. DNS-rebinding residual; `cargo audit` + `cargo deny` pass. **Correction 2026-09-26: the last clause is not merely unpassed, it is unconfigured** — there is no `deny.toml`, no `audit.toml`, and no `.cargo/config*` anywhere in the repo. PASETO is still unimplemented and self-declared future work (`wl-relay/src/auth.rs:2`, `wl-protocol/src/lib.rs:4`). Partial credit: `wl-core/src/net.rs:1-79` is a genuinely thorough SSRF policy (schemes, embedded creds, query/fragment, GCE hostname + subdomains, IPv4-mapped IPv6, link-local v6) with the DNS-rebinding TOCTOU residual explicitly documented at `:15-19` and covered by 5 tests (`:88-141` — `accepts_https_and_lan_http`, `rejects_non_http_schemes`, `rejects_credentials_query_fragment_and_metadata`, `allows_loopback_lan_and_ula`, `rejects_garbage`) — but per CORE-7(a) it is only invoked from the shell, never from `Repos::save_settings`.
- [ ] **SHIP-3 — Perf budgets.** Canvas re-render <16ms on outbox update; zero idle GPU (no infinite glow/blur); release wasm ≤1 MB; relay p95 push/pull latency SLO + 100k-cap load test (see P4). **PREMISE CORRECTED 2026-09-26: there is no wasm size guard in this repo.** The plan cited `prune-dx-dist.sh` as one, but that script enforces **no byte budget at all** — it only deletes content-hashed `*-dx*` js/wasm assets unreachable from `index.html` (`prune-dx-dist.sh:24-47`), has no `du`/`stat`, and always exits 0. It is a *staleness pruner* (it exists to stop dead bundles accumulating ~800 KiB per rebuild, PRD-DELTAS #72), not a size gate. To satisfy this item you must **write** the ≤1 MB assertion (a `stat`-based check in `build-ui.sh` is the natural place) rather than tick an existing guard. Still absent: any p95 latency SLO doc, any latency load test (`wl-relay/tests/adversarial.rs:453-481` covers the 100k cap only), and any 16ms frame-budget test.
- [ ] **SHIP-4 — Mobile-ready (deferred, approved delta #8).** No Android/iOS scaffolding in v1. **Static platform-clean gate PASSES (re-verified 2026-09-26):** `wl-core/Cargo.toml:12-30` declares no tokio, Tauri, reqwest, axum, or hyper — only crypto/serde/chrono/url/rusqlite; tokio appears only as a dev-dependency (`:33`) and is in fact entirely unused; zero `tokio::`/`tauri::`/`reqwest::` references in `wl-core/src`. That grep gate is the part a future agent can actually run — keep it. Remaining work is human/toolchain-gated: `tauri android/ios init` smoke when toolchains exist.
- [ ] **SHIP-5 — Docs.** PRD (missing from `docs/` — only deltas exist, now #1–#76; recover or declare deltas canonical); user runbook (install → open app directly to Home (no onboarding) → hamburger drawer → Settings → save 12-word phrase + enter API key for one of 4 providers [Openrouter/Google/Qwen/bytez] → exit Settings → drawer → create goal → active directive on canvas → check-in → sync); drawer design spec (left slide, dimmed overlay, App name top, 2 horizontal icon buttons); operator runbook (relay env `WL_RELAY_DB/ADDR/PG_URL`, quotas, TLS proxy, backups — relay blobs are opaque, loss = data loss). **Premise corrected 2026-09-26:** the sub-clause "README verification-status table refresh (stale '80 pass')" was already satisfied by the earlier P0-HYGIENE-2 pass — the table read 162/15/10 and has now been corrected to 164/17/11. The genuinely open part is the **missing PRD** and the **three absent runbooks** (no file matching `*runbook*` exists, and no `docs/` file mentions one). Drawer design spec is effectively now captured in MVP-1/UI-4 above — extract it to `docs/` when writing the rest.

---

## Appendix A — Bug registry (quick index)

| ID | Severity | Location | One-liner |
|----|----------|----------|-----------|
| B-001 | P0 | `wl-app/src/commands.rs:853` vs `ui/.../byok.rs:14` vs `wl-core/.../domain.rs:370` | Providers not aligned to 4-only (Openrouter/Google/Qwen/bytez); Anthropic/OpenAI still in adapter/core/UI |
| B-002 | P0 | `ui/.../goal_create.rs` | Manual goal dead end, 0 milestone/directive authoring UI |
| B-003 | P0 | `wl-sync/src/sync.rs:235` | Deletes never sync (no tombstone path) |
| B-004 | P1 | `ui/.../seed_vault.rs` + `commands.rs:31` | Impossible "Create new" button when locked |
| B-005 | P1 | `ui/.../morning_brief.rs` | Briefing discards IDs, assumes persistence |
| B-006 | P1 | `ui/.../canvas.rs:70` | Check-in gated ≥19h, no other route |
| B-007 | P1 | `wl-app/src/hotkey.rs:22` | Unregister-before-validate kills summon |
| B-008 | P1 | `wl-sync/src/sync.rs` validate vs apply | Unknown-table wedges sync |
| B-009 | P1 | `wl-sync/src/sync.rs` apply guards | SQL LWW missing op_id tie-break |
| CORE-1 (E8) | P1 | `wl-core/src/engine/mod.rs:173` | `blocked` no unblock; `skipped` never completes milestone |
| CORE-2 (E7) | P2 | `wl-core/src/engine/velocity.rs:82` | Adjustment displayed, never applied |
| CORE-3 | P1 | `wl-core/src/store/repo.rs` goals | No `set_goal_status`, multi-active goals |
| CORE-4 | P2 | `wl-core/src/store/repo.rs` identity | N-row `identity_config` |
| CORE-5 | P2 | `wl-core/src/crdt/mod.rs:120` | Poison-op mutates on reject |
| CORE-6 | P2 | `wl-core/src/engine/mod.rs` phases | Missing-phase fail-closed, no repair |
| CORE-7 | P2 | various (see Phase 4) | Settings URL / outbox cap / plan-count / dedup gaps |
| SYNC-1 (P4) | P2 | `wl-relay/src/store.rs` | Push holds global mutex/txn whole batch |
| SYNC-2 | P2 | `wl-relay/src/store.rs` quotas | 100k cap cliff, no GC |
| SYNC-3 | P2 | `wl-relay` pg vs sqlite | Index divergence, feature-combo fragility |
| SYNC-4 | P2 | `wl-protocol/src/lib.rs` | Size-bound skew, dead `MAX_PULL_BYTES` |
| SHELL-1 | P2 | `repo.rs` + `commands.rs` | Mutex-poison fail-stop |
| SHELL-2 | P2 | `wl-app/src/relay.rs:10` | `spawn_blocking` footgun |
| UI-1…UI-7 | P2 | `ui/src/...` | Idle-coercion, modal, staleness, discovery, copy, telemetry, a11y |
| AI-1 | P2 | `wl-core/src/ai/dispatch.rs` | Plan persist not atomic |
| HYGIENE | P0 | `Plan/Frontend.md`, `README.md`, `Plan/AGENT.md` | Deleted spec, stale counts |

### Added by the 2026-09-26 audit (see the Audit section above)

| ID | Severity | Location | One-liner |
|----|----------|----------|-----------|
| AUDIT-1 | P2 | `ui/src/app.rs:190`, `ui/src/screens/byok.rs` | `Screen::ByokSetup` unreachable; its provider pills are dead code and diverge from `settings.rs`'s `<select>` |
| AUDIT-2 | P3 | `wl-sync/src/sync.rs:700-707` | Docstring claims a "table allow-list" that B-008 removed and the NOTE at `:732` denies |
| AUDIT-3 | P2 | `wl-core/src/store/repo.rs:690-790` | Six `delete_*` tombstone writers have integration coverage only, no `wl-core` unit test |
| AUDIT-4 | P2 | `AGENTS.md` | "Secrets never touch the DOM" is unachievable for user-typed input; binding rule needs an owner decision |
| AUDIT-5 | P1 | `wl-relay/src/{store,auth}.rs`, `wl-core/src/hlc.rs` | 14 of SHELL-1's ~65 poison sites sit in the long-lived relay and the HLC hot path — one bad holder kills a server |
| AUDIT-6 | P3 | `crates/wl-core/src` (0 occurrences) | Zero `debug_assert` in the whole crate, yet three plan items ask for one |
| AUDIT-7 | P2 | `wl-core/src/engine/velocity.rs:82` | `estimate_adjustment` is computed, shipped, displayed, and read by nobody |

### Corrections applied to this plan by that audit

| Item | Correction |
|------|-----------|
| Counts | 187/162/15/10 → **192/164/17/11** (this file, `README.md`, `AGENTS.md`, `Plan/AGENT.md`); `wl-core` units 120 → 112 |
| MVP-1, MVP-2, UI-4, AI-3 | Ticked — verified closed but stale-unticked |
| MVP-3, MVP-4, MVP-6, SHELL-2, SHELL-4, SHELL-5, UI-5, UI-6, AI-2 | Re-scoped `[ ]` → `[~]` with per-clause status |
| SHELL-1 | "~30 sites" → **~65** (49 `unwrap` + 16 `expect`), full file:line list added; `into_inner()` recovery is zero repo-wide |
| SHIP-3 | Premise fixed: `prune-dx-dist.sh` is a **staleness pruner, not a size guard** — no byte budget exists to tick |
| SHIP-5 | Premise fixed: the README's "80 pass" was already corrected by P0-HYGIENE-2 |
| SHIP-2 | Strengthened: `cargo audit`/`cargo deny` are **unconfigured**, not merely unpassed |
| B-004 | Noted that the new copy guard is **unreachable dead code** (`settings.rs:104` hardcodes `has_identity: false`) and untested |
| B-006 | Noted that only 1 of the 3 promised routes shipped (telemetry only) |
| B-009 | Residual severity raised: the ordinary offline-catch-up path reproduces it, not just "after a hard-stop"; no cursor reset exists in the shell |
| SYNC-4 | `MAX_PULL_BYTES` confirmed fully dead; `MAX_RESPONSE_BYTES` confirmed unused by the relay response path |
| PRD-DELTAS | #1–72 → **#1–77** |

## Appendix B — Design constraints (never violate; from skill + AGENTS.md)

- 420×747 non-maximizable; exactly ONE directive on canvas; no future-task lists.
- `#131312/#20201F/#DAD5C7`, coral `#E26D52` timer/beacon ONLY; no `#000/#FFF`, no red failure, no streaks. Skips = "velocity adjustments" (GPS metaphor).
- Escape requires reason categorization before unmount. `⌘/Ctrl+Enter` complete, `Esc` bailout, `Ctrl+,` telemetry.
- Drawer: left slide (`~65%` width), full-screen dimmed overlay (`rgba(19,19,18,0.85)`), NOT full-screen; top App name (`DM Sans` bold `#DAD5C7`), bottom 2 buttons HORIZONTAL: Goal creation LEFT (creation icon + contrasting/highlight), Settings RIGHT (gear icon). Only icons; accessible labels. Closes on Esc / backdrop / tap.
- Empty-state (no directive): "No active directive." — no red, no streak, no backlog list. Optional subtle link to hamburger → create goal.
- Secrets never reach SQLite, the relay, or logs (single mandated mnemonic display excepted, cleared after). **Note: the older phrasing "never touch the DOM" is unachievable for user-typed input in a webview and was removed as a false claim — see P0-HYGIENE-5 / AUDIT-4.** Relay never sees plaintext (seal `table:record` AAD). HLC on every mutation. `wl-core` stays platform-clean. Deltas → `docs/PRD-DELTAS.md`.
- The vault is a local encrypted file (`0600`, XChaCha20-Poly1305, 32-byte CSPRNG key, KDF work factor 0) — **not** an OS keychain and **not** hardware-backed. Do not describe it as either (SHELL-6 / PRD-DELTAS #19, #77).

## Appendix C — Suggested agent execution order (re-sequenced 2026-09-26)

Phase 0 and Phase 1 are **done** (all 9 bugs closed and re-verified). MVP-1,
MVP-2, UI-4 and AI-3 also landed. Remaining work, highest value first:

1. **AUDIT-5 + SHELL-1 (relay/hlc subset)** — 14 poison sites can fail-stop a
   long-lived server. Cheapest real risk reduction on the board. Do first.
2. **Phase 4 CORE-1…CORE-7** — engine/store correctness, all unit-testable in
   this sandbox. CORE-5 (poison-op mutates on reject) and CORE-7 are the
   cheapest real bugs; CORE-1 (no unblock path) is the only P1.
3. **Finish the `[~]` items** — MVP-3, MVP-4, MVP-6, SHELL-2, SHELL-4, SHELL-5,
   UI-5, UI-6, AI-2. Each has a short remaining-clause list inline; several are
   one-line copy or one-attribute fixes (AUDIT-1, AUDIT-2, AUDIT-6).
4. **Phase 6 UI-7 (a11y)** — focus trap, `aria-live`, timer label, `?` map.
   Self-contained, no shell changes.
5. **MVP-6 live gate** — run `cargo tauri dev` and record a real command
   transcript. Needs a display; do not tick it otherwise.
6. **Phase 3 SYNC-1…SYNC-8** — needs load + Postgres verification.
7. **Phase 5 remainder + Phase 7 AI-1/AI-4** — `PARALLEL-OK` after 2–4.
8. **Phase 8 SHIP-1…SHIP-5** — human hardware, docs, sign-off. SHIP-2 and SHIP-3
   each need something *written* (audit config, size assertion), not just run.
